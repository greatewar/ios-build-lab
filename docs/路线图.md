# 路线图

## Phase 1：底座

- [x] 建公开仓库
- [x] 接入本地 Git
- [x] 建第一版 GitHub Actions
- [x] 建脚本目录和文档目录
- [x] 跑通首轮 Theos bootstrap smoke test

## Phase 2：通用化一点，但不过度设计

- [x] 抽出通用 Theos 环境准备脚本
- [x] 支持 workflow_dispatch 输入外部仓库地址
- [x] 支持指定 ref / 子目录 / 构建命令
- [x] 统一 artifacts 输出目录
- [x] 跑通外部项目构建样例

验收记录：GitHub Actions Run `29848150385` 成功，artifact `external-theos-29848150385` 包含构建日志、摘要和 `com.greatewar.ios-build-lab-hello_1.0.0_iphoneos-arm.deb`。

## Phase 3：真实项目接入

- [x] 接入 TrollStore 核心发布包构建验证
- [ ] 补充 TrollStore installer IPA 输入与构建验证
- [ ] 评估是否需要 Docker 版 runner
- [ ] 评估是否需要 release / artifact 自动命名

验收记录：GitHub Actions Run `29851087119` 于 `2026-07-21` 成功，artifact `trollstore-2.1.1-29851087119` 包含 `build.log`、`build-summary.txt`、`trollstore-source-commit.txt`、`trollstore-submodules.txt` 与 `_build/TrollStore.tar`。其中 `TrollStore.tar` 大小 `2,426,000` bytes，SHA256 `7775f771651f3afe9dd3aa6cc0a560eb0715d053fa81bdfe265eab57316d0051`。

## Phase 4：TrollStore API 变体

- [x] 增加 TrollStore 本地 API overlay
- [x] 跑通 API 版 TrollStore 编译
- [x] 产出可直接安装的 `.tipa`
- [ ] 上真机验证 `health/apps/install/uninstall/open`

验收记录：GitHub Actions Run `29855945988` 于 `2026-07-21` 成功，artifact `trollstore-2.1.1-29855945988` 包含 `_build/TrollStore.tar` 与 `_build/TrollStore-local-api.tipa`。其中 `TrollStore-local-api.tipa` 大小 `2,462,236` bytes，SHA256 `5532685d35306ea5709ac90bf776d59b83703d4233c87f3daceefd72b7905ece`。

## 不急着做

- 多平台全家桶
- 复杂签名体系
- 过早抽象成“大一统 iOS 编译平台”
